{
    "name": "tool-gen-level-snake-out-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"cd server && ../venv/bin/python run.py\" \"cd client && npm run dev\"",
        "install:all": "npm install && cd client && npm install && cd .. && source venv/bin/activate && pip install -r server/requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}